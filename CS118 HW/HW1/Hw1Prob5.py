'''
1) lists are mutable, and when passed as function arguments the address location of the list itself it passed, not just
a copy of the values, therefore it is permanently changed outside the function.

2) An index error is raised

For some reason the autograder expects an explanation involving lambda functions despite the quesiton not being about lambda functions!!! 
Hopefully adding this extra line resolves the issue!
'''